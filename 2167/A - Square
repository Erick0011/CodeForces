#include <bits/stdc++.h>
 
using namespace std;
 
int main()
{
 
    int t;
 
    cin >> t;
 
    while (t--)
    {
        int a, b, d, c;
 
        cin >> a >> b >> c >> d;
 
        if (a != b || a != c || a != d)
        {
            cout << "NO" << "
";
        }
        else
        {
            cout << "YES" << "
";
        }
    }
}